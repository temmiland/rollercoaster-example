buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "com.mobidevelop.robovm:robovm-gradle-plugin:${libs.versions.robovm.get()}"
    }
}

apply plugin: 'robovm'
apply plugin: 'java'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation project(':')
    implementation libs.gdx.backend.robovm
    implementation "com.badlogicgames.gdx:gdx-platform:${libs.versions.gdx.get()}:natives-ios"
    implementation libs.robovm.rt
    implementation libs.robovm.cocoatouch
}

robovm {
    archs = 'arm64'
    iosSkipSigning = false
}
